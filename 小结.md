## 已完成的工作
- 读取layout & pattern文件，储存为运算所需要的结构体
- 将曼哈顿图形的顶点坐标信息转换为边向量的长度与方向信息，将四次空间旋转变化对应的匹配情况搜索减少为一次
- 完成了pattern文件的镜像操作，与原pattern进行一次同样的运算即可实现对八种空间变换情况的全部搜索
- 使用pattern中完全在marker内部的曼哈顿图形作为搜索对象，初步找出潜在匹配区域
- 对每层layout中的曼哈顿图形的最小包围矩形构建RTree，快速搜索出与潜在匹配区域相交的图形
- 利用布尔与运算裁剪出layout图形中在潜在匹配区域之内的部分，对于空心曼哈顿图形被裁剪为两个相邻曼哈顿图形的情况，使用布尔或运算合并
- 将layout文件中裁剪出的潜在匹配区域与原始pattern文件进行异或比较，输出匹配情况
## 存在的问题
- 初步搜索潜在匹配区域的方法可能会遗漏一些部分匹配的情况
- pattern文件的空间变换后的顶点坐标信息混乱，以至于未能正确完成所有的异或运算输出
- 尚未实现多线程的正确管理
- 尚未将匹配结果转换为赛题要求的格式输出
- 所需要的布尔运算次数过多，运算速度较慢
## 反思
- 起步时间较晚，且穿插考试、大作业等其他任务，投入精力不足，导致未能完成
- 前期调研进度未跟上代码撰写速度，例如遇到瓶颈后才开始观察版图结构与pattern特性、考虑库调用难度与树搜索适用情况等问题，代码结构调整与沉没成本较高
- 分工不明确且中后期有效交流较少，合作效率低
- 平均编程能力有待提高，思路难以用代码实现
