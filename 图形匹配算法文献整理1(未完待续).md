# Geometric Pattern Match Using Edge Driven Dissected Rectangles and Vector Space
https://ieeexplore.ieee.org/document/7422009
## Introduction
### 图形匹配方法综述
#### 基于机器学习
- 方法：提取热点模式来训练人工神经网络并创建监督学习模型，用于预测布局设计中的潜在热点。
- 关键：创建一个准确的学习模型以避免误测。
- 局限性：需要长时间的训练和其他复杂技术来降低误报率。即使误报率相对较低，这种方法中仍然不可避免地会出现误报。
>[4] D. Ding, X. Wu, J. Ghosh, and D. Z. Pan, “Machine learning based lithographic hotspot detection with critical-feature extraction and classification,” in Proc. IEEE Int. Conf. IC Design Technol. (ICICDT), Austin, TX, USA, 2009, pp. 219–222.  
>[5] D. Ding, A. J. Torres, F. G. Pikus, and D. Z. Pan, “High performance lithographic hotspot detection using hierarchically refined machine learning,” in Proc. ASP-DAC, Yokohama, Japan, 2011, pp. 775–780.  
>[6] J.-Y. Wuu, F. G. Pikus, A. Torres, and M. Marek-Sadowska, “Rapid layout pattern classification,” in Proc. ASP-DAC, Yokohama, Japan, 2011,pp. 781–786.
#### 基于显式模型
- 方法：创建一个显式模型来比较目标图形与版图。
- 案例：Kahng _等人_ [3] 提出创建一个dual graph来表示图形，用于过滤掉不匹配的图形。
- 局限性：由于其固有的建模误差，容易产生误测。但比基于机器学习的方法更加准确和高效。
>[3] A. B. Kahng, C.-H. Park, and X. Xu, “Fast dual graph based hotspot detection,” in Proc. SPIE, vol. 6349. Monterey, CA, USA, 2006, pp. 628–635.  
>[11] J. Guo, F. Yang, S. Sinha, C. Chiang, and X. Zeng, “Improved tangent space based distance metric for accurate lithographic hotspot classification,” in Proc. ACM/IEEE DAC, San Francisco, CA, USA, 2012, pp. 1169–1174.  
>[12] S.-Y. Lin, J.-Y. Chen, J.-C. Li, W.-Y. Wen, and S.-C. Chang, “A novel fuzzy matching model for lithography hotspot detection,” in Proc. ACM/IEEE DAC, Austin, TX, USA, 2012, pp. 1–6.
#### 基于字符串
- 方法：应用字符串匹配技术进行图形匹配。首先，创建一个网格。每个网格点被转换到一个布局矩阵，当它与几何形状重叠时赋值为 1，否则为 0。然后，将点编码成字符串进行匹配。
- 局限性：虽然不会出现误测，但由于制造网格尺寸越来越小，所需的计算时间指数级增长，不适合前沿设计。
>[7] H. Yao, S. Sinha, C. Chiang, X. Hong, and Y. Cai, “Efficient process hotspot detection using range pattern matching,” in Proc. ICCAD, San Jose, CA, USA, 2006, pp. 625–632.  
>[8] J. Xu, S. Sinha, and C. C. Chiang, “Accurate detection for process hotspots with vias and incomplete specification,” in Proc. ICCAD, San Jose, CA, USA, 2007, pp. 839–846.  
>[13] H. Yao et al., “Efficient range pattern matching algorithm for process hotspot detection,” IET Circuits, Devices Syst., vol. 2, no. 1, pp. 2–15, Feb. 2008.
#### 基于 DRC 
- 方法： DRC 规则来识别模式。首先，输入图形被转换为多个 DRC 规则，然后分析这些规则的输出来获得图形匹配。
- 局限性：虽不会产生误报，它可以用于精确匹配。然而，随着模式数量的增加，它倾向于生成大量的复杂 DRC 规则，导致计算成本高。
>[1] Y.-T. Yu, Y.-C. Chan, S. Sinha, I. H.-R. Jiang, and C. Chiang, “Accurate process-hotspot detection using critical design rule extraction,” in Proc. DAC, San Francisco, CA, USA, 2012, pp. 1163–1168.  
>[9] F. Pikus, “Topological pattern matching,” U.S. Patent 018 594 A1, Jul. 2010.  
>[10] F. Gennari, “Fast pattern matching,” U.S. Patent 7 818 707, Oct. 2010.
### 本文工作
- 基于相对来说更快且更准确的DRC方法图形匹配，提出了一种基于 DRC 的新型图形匹配方案，更快，更加直观简单、更易理解与描述模式，适合并行运算。
- 使用简化的边缘驱动 DRC 规则和数学空间向量概念，以实现更快、更准确的结果，并将匹配所有可能模式方向所需的迭代次数从 8 次减少到最多 2 次。

## 背景
### DRC (Design Rule Checks)
 - **两个最基本规则：宽度规则和间距规则**
1. 宽度规则
	- 为任何形状定义最小宽度来防止窄形状的夹断。
2. 间距规则
	- 定义两个形状之间的最小允许距离来防止桥接。

- 表达方式：可以使用常数值或带有变量的方程/不等式表达。DRC 工具通过提供违规边缘的位置来报告这些规则的违规情况。
- 其他 DRC 规则：如面积规则、比例规则、重叠规则和密度约束规则等。


### Edge Operation 边缘运算
- DRC工具中检查边缘相关规则的基本操作，包括长度（LENGTH）、宽度（WIDTH）、间距（SPACE）和角度（ANGLE）

## 基于边缘驱动的剖分矩形图形匹配

### 图形描述
- 根据边缘的长度和宽度，或者边缘的长度和间距创建成员矩形
```
<Name of member>
Len_1 == value (μm or any user unit)
Len_2 == value
Width == value or Space == value.
```
- 利用这些信息，执行 DRC 操作，如 LENGTH、WIDTH 或 SPACE 来生成成员矩形。
- 对于可能有不同分解方式的图形，执行额外的 DRC 操作 ANGLE，以强制执行单一的分解方式（参见Appendix A中Example 2）
![[Pasted image 20241011163549.png]]
- 可选取任意成员作为origin member，其余一个作为reference，以各自中心点信息创建空间信息用于图形描述，包括：
1. 向量（角度+距离）
2. 象限位置（四象限或坐标轴之一）
3. 每个member中心到总边界框的距离
4. 宽度与高度
5. 布尔值（指示宽度or空间矩形？）
- 每个成员的上述信息储存在pattern description database (PDB)中，由此可区分出8种方向（四种旋转x两种翻转），避免八次迭代
![[Pasted image 20241011164819.png]]
#### 其他
- We also store in PDB Len_1, Len_2, Width or Space value for each member as well as their corresponding relational operator for fuzzy pattern match which will be discussed in Section III-E.
- The only exception is when the vector direction is along-axis or when the angle is 45°. In these cases, we have to iterate two times to cover two cases during the pattern matching process. Fig. 6 explains why we need to iterate two times in the case of along axis.

### 图形匹配

#### Algorithm 1 EDDR_PM
- 暴力算法，逐个访问布局中的所有原始成员
- 如果边界框内有非成员多边形（Non-member=Metal_1 NOT (OR P1 P2 P3 P4 P5 P6).），或者成员数不同，则立即将其分类为不匹配。
- 伪代码：  
	1: **procedure** EDDR–PM(P1, P2, …., Pn, nonMem, PDB)  
	2:P1 = set of origin members in a layout  
	3:P2 = set of the first reference members in a layout  
	4:P3..n = set of all the other reference members in a layout  
	5:nonMem = set of nonmember polygons in a layout  
	6:PDB = a pattern description database.  
	7:**while** !empty in P1 **do**  
	8:Find a reference member p2 in P2 by searching the vector distance  
	9:between P1’s center and P2’s center.(PDB has this info.)  
	10:**if** found **then**  
	11:**if** the found p2 a valid reference member **then**  
	12:Create a bounding box using d1, d2, d3, and d4 determined by  
	13:vector info between P1 and the found P2’s center.  
	14:**else**  
	15:No match. **continue to next p1 in P1**  
	16:**if** nonMem exists inside the bounding box **then**  
	17:No match. **continue to next p1 in P1**  
	18:Find other members in P3⋅⋅⋅Pn inside the bounding box.  
	19:**for** each member inside the bounding box **do**  
	20:n = number of each member inside bounding box  
	21:m = number of each memeber described in PDB  
	22:**if** n != m **then**  
	23:No match. **continue to next p1 in P1**  
	24:**if** valid member == false **then**  
	25:No match. **continue to next p1 in P1**  
	26:Matching pattern found at this point.  
	27:Output the bounding box to indicate the match.  
	28:**continue to next p1 in P1**  
	29:**else**  
	30:No match. **continue to next p1 in P1**  
- 步骤（8）和（18）使用了基于扫描线的拓扑检查，这在运行时间性能上并不理想。
#### Algorithm 2 EDDR_PM
- 通过采用二分搜索网格算法来改进暴力算法  
	1:**procedure** EDDR–PM(P1, P2, …., Pn, nonMem, PDB)  
	2:Inputs (P1..n, nonMem, and PDB) are the same as Algorithm 1.  
	3:ADD_BIN for each member from P1 to Pn.  
	4:LOCATE_BIN for all the origin members of P1 and get bin_counts  
	5:**for** i=1→bin_counts for pi in P1 bins **do**  
	6:LOCATE_BIN a reference member, p2, in P2 bins by searching  
	7:the vector distance between P1’s center and P2’2 center.(PDB has this info.)  
	8:**if** found **then**  
	9:Same process as Algorithm 1 to decide match or no match  
	10:…..  
	11:LOCATE_BIN for other members inside the bounding box.  
	12:…..  
	13:Same process as Algorithm 1 to decide match or no match  
	14:…..  
	15:**else**  
	16:No match. **continue**  
- 我们使用如 III-A 节所述的 PDB 格式信息进行有效成员检查和创建边界框。在算法的第（12）步中，我们可以通过计算 P1 和找到的 P2 之间的向量（角度和距离），并参考 PDB 中的信息，确定我们想要的八个可能方向之一。如果其平面沿轴线或其角度为 45°，我们将创建两个边界框，并在算法中对每个边界框进行后续检查。
